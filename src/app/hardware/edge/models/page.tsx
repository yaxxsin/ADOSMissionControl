"use client";

/**
 * @module HardwareEdgeModelsPage
 * @description Model list route for the ADOS Edge transmitter.
 * @license GPL-3.0-only
 */

import { ModelList } from "@/components/hardware/transmitter/ModelList";

export default function HardwareEdgeModelsPage() {
  return <ModelList />;
}
